const { Comment } = require("../models");

const newComment = {
  comment_text: "Me boy what are you doing?",
  user_id: 5,
  post_id: 3,
};

const seedComments = () => Comment.create(newComment);

module.exports = seedComments;